import CustomInput from "../tailwind/CustomInput/CustomInput";
import styles from "./style.module.css";

const Playground = () => {
  return (
    <div className={styles.wrapper}>
      <CustomInput />
    </div>
  );
};

export default Playground;
