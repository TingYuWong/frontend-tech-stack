import styles from "./style.module.css";
import CustomInput from "@/components/ant/CustomInput/CustomInput";

const Playground = () => {
  return (
    <div className={styles.wrapper}>
      <CustomInput />
    </div>
  );
};

export default Playground;
