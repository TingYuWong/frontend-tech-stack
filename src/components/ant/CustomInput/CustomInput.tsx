import { Input } from "antd";

const CustomInput: React.FC = () => (
  <Input
    placeholder="Basic usage"
    style={{
      height: "80px",
      borderRadius: "8px",
      fontSize: "30px",
      fontWeight: 600,
    }}
  />
);

export default CustomInput;
